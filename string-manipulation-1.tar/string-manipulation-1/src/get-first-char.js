/* exported getFirstChar */
/**
 * PSEUDOCODE:
 *
 * Define a function, named `getFirstChar`, that accepts one argument, `string`.
 *   Declare a constant variable named `firstChar`, and assign it to the string argument at the first string
 * index.
 * Return the value of firstChar.
 **/

/**
 * CODE IMPLEMENTATION:
 **/
function getFirstChar(string) {
  const firstChar = string[0];
  return firstChar;
}
